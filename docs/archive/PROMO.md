# Deep Search Promo Kit

## One-Line Pitch

Deep Search is a local-first deep research skill that turns a vague prompt into a defensible multi-source investigation.

## Short Description

Deep Search packages a serious research workflow into a reusable local skill. It combines broad web coverage, news aggregation, academic lookup, repo and code signals, and structured synthesis so your agent can investigate a topic instead of just summarizing the first page of results.

## GitHub Repo Subtitle

Local-first deep research skill for serious multi-source investigation

## GitHub About Text

Local-first deep research skill with multi-source search, news, academic lookup, and structured synthesis.

## Chinese Intro

`deep-search` 是一个偏本地优先的深度调研 skill。它不是“搜一下然后总结”的轻量工具，而是把多源搜索、新闻聚合、学术资料检索、代码与仓库信号、交叉验证和结构化输出串成一条完整工作流，适合需要高覆盖率、低盲区和可复用流程的信息调研任务。

## English Intro

`deep-search` is a local-first deep research skill built for exhaustive investigation. Instead of stopping at a shallow web summary, it orchestrates multi-source search, news aggregation, academic lookup, repo and code signals, and structured synthesis into a repeatable research workflow.

## Launch Post - Chinese

我把自己本地一直在打磨的 `deep-search` skill 开源了。

它的目标不是“更快给答案”，而是“让结论更站得住”。

这个 skill 适合下面几类任务：

- 需要全网多源交叉验证的调研
- 需要把新闻、社区讨论、学术资料、代码仓库放在一起看
- 不想把 deep research 完全交给黑盒 SaaS
- 想把研究流程沉淀成可复用、可迭代的本地 skill

仓库里包含：

- `SKILL.md` / `DEEP_SEARCH.md` 执行协议
- 环境安装和依赖检查脚本
- 配套的 news / academic / search cluster 子技能
- 可继续扩展的本地研究框架

如果你也在折腾 agent research、情报收集、竞争分析或者内容调研，这个仓库应该能直接拿去改，也欢迎 star 一下。

## Launch Post - English

I just open-sourced my local `deep-search` skill.

The goal is not "faster answers." The goal is "stronger conclusions with fewer blind spots."

This repo is built for research tasks that need:

- broad multi-source coverage
- cross-validation across different source types
- a workflow that combines web search, news, academic material, and repo/code signals
- a local-first setup instead of a fully hosted black-box tool

It includes the core skill protocol, setup scripts, dependency checks, and bundled supporting skills for a more complete research stack.

If you are building agentic research workflows, competitive intelligence pipelines, or serious topic investigation tools, this repo should be useful. If it helps, give it a star.

## X / Twitter Version

Open-sourced my local `deep-search` skill.

It is built for research tasks where shallow summaries are not enough:

- multi-source search
- news aggregation
- academic lookup
- repo and code signals
- structured synthesis
- local-first workflow

Goal: stronger conclusions, not just faster answers.

## WeChat / Xiaohongshu Version

最近把自己本地一直在打磨的 `deep-search` skill 开源了。

它更像一个“深度调研工作流”，不是普通搜索增强。

核心思路是把：

- 全网搜索
- 新闻聚合
- 学术资料检索
- 社区讨论补充
- 代码与仓库信号
- 结构化总结

放进同一条 agent workflow 里，尽量减少信息盲区，让结论更稳。

如果你平时会做：

- AI / 工具 / 公司调研
- 竞品分析
- 热点事件追踪
- 内容选题研究

这个仓库应该挺适合直接拿去改，也欢迎点个 star。
